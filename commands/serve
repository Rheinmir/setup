#!/bin/bash
# Description: Start a lightweight HTML server for the library
# Usage: ./commands/serve

PORT=${1:-8000}
echo "🚀 Starting HTML Library Manager at http://localhost:$PORT"
echo "Press Ctrl+C to stop."

cd html && python3 -m http.server $PORT
