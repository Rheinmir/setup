# wiki/sources/

Distilled references and decision records: summaries of external docs, ADRs, paper takeaways — derived from files in `raw/`.

- `draft/` — proposals created by the `propose` skill that have not yet been implemented.

Use `_template.md` as the starting format for new files.
