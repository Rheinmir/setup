# wiki/sources/draft/

Feature proposals created by the `propose` skill. Files are named `DDMMYY-feature-name-module.md`.

A draft stays here until the feature is committed. After `verify-before-commit` succeeds, the draft is promoted to a permanent page in `concepts/`, `entities/`, or `sources/`.

Use `_template.md` as the starting format for new files.
